---
import ButtonLink from './ButtonLink.astro'
import Icon from './Icon.astro'
import WorldIcon from '../icons/WorldIcon.astro'
import GithubIcon from '../icons/GithubIcon.astro'
import JavaScriptIcon from '../icons/JavaScriptIcon.astro'
import CSSIcon from '../icons/CSSIcon.astro'
import BoostrapIcon from '../icons/BoostrapIcon.astro'
import HTMLIcon from '../icons/HTMLIcon.astro'
import MySqlIcon from '../icons/MySqlIcon.astro'
import PHPIcon from '../icons/PHPIcon.astro'
import TailwindIcon from '../icons/TailwindIcon.astro'
import AstroIcon from '../icons/AstroIcon.astro'
import JavaIcon from '../icons/JavaIcon.astro'
import NetBeansIcon from '../icons/NetBeansIcon.astro'
import ReactIcon from '../icons/ReactIcon.astro'


import projects from '../projects.json'
---

{
    <div class="flex flex-wrap">
        {projects.map((data) =>(
            <ButtonLink id={data.id} tittle={data.tittle}></ButtonLink>        
        ))}
    </div>
}

<section id="numberProjects" numberProjects={projects.length} class="   relative w-full  h-full lg:h-auto flex">
        
    {
        projects.map((data) =>(
            <div id={`ContainerDetails${data.id}`}  class="hidden lg:w-11/12 w-full mx-auto  relative my-2 overflow-hidden  flex flex-wrap md:flex-row row-span-1 px-10 py-8 bottom-0 rounded-md bg-slate-200 dark:bg-slate-100/20 dark:text-white text-black">
                <div class="w-full md:w-8/12 ">
                    
                    <h3 class="text-2xl ">{data.tittle}</h3>
                    <div class="md:w-10/12 w-full">
                        <p class="mx-auto">{data.description}</p>
                        <div class="flex pt-1 flex-col md:flex-row ">
                            <span class="w-full md:w-6/12 flex " >
                                <Icon link={data.url} >GitHub <GithubIcon/></Icon>
                                { data.urlweb && <Icon link={data.urlweb}  class="flex"><p>Web</p> <WorldIcon/></Icon>}
                            </span >
                            <span class="w-6/12 flex justify-start   lg:justify-end pr-1 text-slate-800 dark:text-violet-300">
                                {
                                    data.technologies.map((svgs) =>(
                                        svgs === "javascript" && <JavaScriptIcon /> ||
                                        svgs === "css" && <CSSIcon /> ||
                                        svgs === "html" && <HTMLIcon /> ||
                                        svgs === "bootstrap" && <BoostrapIcon /> ||
                                        svgs === "mysql" && <MySqlIcon /> ||
                                        svgs === "sqlite" && <MySqlIcon /> ||
                                        svgs === "php" && <PHPIcon /> || 
                                        svgs === "tailwind" && <TailwindIcon /> ||
                                        svgs === "astro" && <AstroIcon /> ||
                                        svgs === "java" && <JavaIcon /> ||
                                        svgs === "netbeans" && <NetBeansIcon /> ||
                                        svgs === "react" && <ReactIcon /> 

                                    ))
                                }
                            </span>
                        </div>
                    </div>
                    
                </div>
                <img class="w-full md:w-4/12 p-1 top-0 shadow-xl h-fit " src={data.img} alt={"Imagen de " +data.tittle}>
            </div>                    
        
        ))
    }
</Section>